apiVersion: v1
kind: Service
metadata:
  name: {{ include "pedro-discord.fullname" . }}
  labels:
    {{- include "pedro-discord.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pedro-discord.selectorLabels" . | nindent 4 }}